import 'package:flutter/material.dart';

class S_list extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(), body: Text("スケジュール一覧画面"));
  }
}
